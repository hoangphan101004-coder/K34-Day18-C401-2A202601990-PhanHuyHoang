# cp .env.example .env
# Required for M4 (RAGAS eval) and M5 (Enrichment LLM calls)
OPENAI_API_KEY=sk-...

# Cost controls: enrichment makes one request per chunk, so it is opt-in.
OPENAI_ENRICHMENT_ENABLED=false
OPENAI_ANSWERS_ENABLED=false
RAGAS_ENABLED=false
